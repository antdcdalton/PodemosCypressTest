# Módulos instalados (se pueden reinstalar a partir de package.json)
node_modules/

# Archivos de configuración local o de entorno (ajustalos según tus necesidades)
.env
.env.local

# Carpetas y archivos de resultados de pruebas
# Cypress genera videos y capturas de pantalla automáticamente al correr las pruebas
cypress/videos/
cypress/screenshots/

# Allure: Resultados y reportes generados
allure-results/
allure-report/

# Archivos y carpetas de logs (p. ej. archivos generados por CI o Jenkins)
*.log
logs/

# Carpetas temporales y caches
tmp/
.cache/
node_modules/.cache/

# Si usas herramientas de empaquetado o compilación, ignora los artefactos generados
/dist
/build

# Otros archivos que no son necesarios en el control de versiones
.DS_Store
Thumbs.db

# Ignora archivos generados por tu IDE (por ejemplo, para IntelliJ)
.idea/
*.iml

# Si usas Jenkins, normalmente la máquina de Jenkins tiene su propio workspace y archivos temporales,
# así que no es necesario incluir archivos que Jenkins genere en el repositorio.
